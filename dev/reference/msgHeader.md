# msgHeader

Message header displaying log level and time stamp

## Usage

``` r
msgHeader(logLevel)
```

## Arguments

- logLevel:

  Log level as a character string (DEBUG, INFO, SUCCESS, WARN, ERROR,
  FATAL)

## Value

A formatted string with the log level and timestamp
