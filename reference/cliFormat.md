# cliFormat

Format text into cli inline format Allows evaluation of expressions
within the text before submitting to logs

## Usage

``` r
cliFormat(..., .envir = parent.frame())
```

## Arguments

- ...:

  Characters to format

- .envir:

  Environment in which to evaluate the expressions

## Value

A formatted character string
