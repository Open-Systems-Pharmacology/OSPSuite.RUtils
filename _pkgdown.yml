url: https://www.open-systems-pharmacology.org/OSPSuite.RUtils/

# uncomment to turn on dark mode
template:
  bootstrap: 5
  # bootswatch: cyborg
  # theme: arrow-dark
  # bslib:
  #   primary: "#0054AD"
  #   border-radius: 0.5rem
  #   btn-border-radius: 0.25rem

reference:

  - title: Enum helpers
    desc: Utilities to work with enum objects
    contents:
      - contains("enum")
      - getEnumKey

  - title: Validation helpers
    desc: Check if the provided object is of certain type
    contents:
      - matches("^validate")
      - matches("^is")
      - matches("^has")

  - title: R6 helpers
    desc: Utilities to work with R6 objects
    contents:
      - Printable

  - title: Miscellaneous
    contents:
      - "`%||%`"
      - ifNotNull
      - matches("^to")
